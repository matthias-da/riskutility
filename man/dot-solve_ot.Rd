% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sinkhorn_internal.R
\name{.solve_ot}
\alias{.solve_ot}
\title{Solve Optimal Transport Matching from Score Cache}
\usage{
.solve_ot(
  score_cache,
  true_idx,
  n_query,
  split_search,
  blk_query,
  epsilon = NULL,
  max_iter = 100L
)
}
\arguments{
\item{score_cache}{list of length n_query. Each element is
\code{list(cand, scores, maximize)}.}

\item{true_idx}{integer vector of true search-side indices.}

\item{n_query}{integer, number of query records.}

\item{split_search}{named list of search-side indices per block.}

\item{blk_query}{character vector of block labels per query record.}

\item{epsilon}{numeric or NULL. Regularization parameter for Sinkhorn.}

\item{max_iter}{integer. Max Sinkhorn iterations (default 100).}
}
\value{
list with \code{risk} (numeric), \code{d_rank} (integer),
\code{transport_plans} (list of matrices per block).
}
\description{
Post-loop handler parallel to \code{.solve_bijective()}.
Builds cost matrices from \code{score_cache}, runs Sinkhorn per block,
and extracts continuous risk scores from the transport plan.
}
\details{
Risk for record i is defined as:
\deqn{risk_i = P_{i, \text{true}} \cdot n_{\text{query}}}
where \eqn{P_{i, \text{true}}} is the transport weight from query
record i to its true match in the search data. Under uniform random
matching, this gives risk = 1/n_search (same baseline as independent
matching).
}
\keyword{internal}
