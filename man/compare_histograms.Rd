% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_histograms.R
\name{compare_histograms}
\alias{compare_histograms}
\alias{compare_histograms.synth_pair}
\alias{compare_histograms.default}
\title{Compare Histograms of a Numeric Variable in Two Datasets}
\usage{
compare_histograms(X, ...)

\method{compare_histograms}{synth_pair}(X, ...)

\method{compare_histograms}{default}(
  X,
  Y,
  num_var,
  cat_vars,
  weight_X = NULL,
  weight_Y = NULL,
  facet_type = "wrap",
  bins = 30,
  transparency = 0.4,
  plot_type = "overlap",
  ...
)
}
\arguments{
\item{X}{A data.frame or data.table containing the original dataset.}

\item{...}{additional arguments passed to methods}

\item{Y}{A data.frame or data.table containing the anonymized/synthetic dataset.}

\item{num_var}{A character string specifying the numeric variable to compare.}

\item{cat_vars}{A character vector of categorical variables used for faceting.}

\item{weight_X}{Optional. A character string specifying the sampling weight variable in X.}

\item{weight_Y}{Optional. A character string specifying the sampling weight variable in Y.}

\item{facet_type}{Character string. The faceting type: \code{"wrap"} (default) for \code{facet_wrap()} or \code{"grid"} for \code{facet_grid()}.}

\item{bins}{Integer. Number of bins for the histogram (default: \code{30}).}

\item{transparency}{Numeric. Transparency level for overlapping histograms (default: \code{0.4}).}

\item{plot_type}{Character string. Type of plot:
\itemize{
\item \code{"overlap"} (default): Overlapping histograms with transparency.
\item \code{"side"}: Side-by-side histograms.
\item \code{"stack"}: Stacked histograms.
\item \code{"separate"}: Separate histograms for each dataset-category combination.
}}
}
\value{
A ggplot2 histogram visualization comparing the distributions.
}
\description{
This function compares the distribution of a numeric variable between
two datasets (e.g., an original dataset and an anonymized/synthetic version)
using histograms. It allows conditional faceting on categorical variables
and supports different visualization styles, including overlapping, side-by-side,
stacked, and separate histograms.
}
\examples{
set.seed(123)
X <- data.frame(
  age = sample(20:80, 500, replace = TRUE),
  gender = sample(c("Male", "Female"), 500, replace = TRUE),
  income = rnorm(500, mean = 50000, sd = 10000),
  weight = runif(500, 0.5, 1.5)
)

Y <- data.frame(
  age = sample(20:80, 1000, replace = TRUE),
  gender = sample(c("Male", "Female"), 1000, replace = TRUE),
  income = rnorm(1000, mean = 48000, sd = 12000),
  weight = runif(1000, 0.5, 1.5)
)

# Overlapping histograms with transparency
compare_histograms(X, Y, num_var = "income", cat_vars = c("gender"),
                   weight_X = "weight", weight_Y = "weight",
                   facet_type = "wrap", plot_type = "overlap")
# Side-by-side histograms
compare_histograms(X, Y, num_var = "income", cat_vars = c("gender"),
                   weight_X = "weight", weight_Y = "weight",
                   facet_type = "wrap", plot_type = "side")

# Stacked histograms
compare_histograms(X, Y, num_var = "income", cat_vars = c("gender"),
                   weight_X = "weight", weight_Y = "weight",
                   facet_type = "wrap", plot_type = "stack")

# Separate histograms
compare_histograms(X, Y, num_var = "income", cat_vars = c("gender"),
                   weight_X = "weight", weight_Y = "weight",
                   facet_type = "wrap", plot_type = "separate")
}
\seealso{
Other comparison: 
\code{\link{ci_overlap}()},
\code{\link{compare_boxplots}()},
\code{\link{compare_chisq_gof}()},
\code{\link{compare_correlation_matrices}()},
\code{\link{compare_distributions_cont}()},
\code{\link{compare_embedding}()},
\code{\link{compare_feature_importance}()},
\code{\link{compare_ks_test}()},
\code{\link{compare_means_frequencies}()},
\code{\link{compare_missing_values}()},
\code{\link{compare_model_performance}()},
\code{\link{compare_multivariate_distribution}()},
\code{\link{compare_multivariate_summary_statistics}()},
\code{\link{compare_outliers}()},
\code{\link{compare_pca}()},
\code{\link{compare_wasserstein}()}
}
\author{
Matthias Templ
}
\concept{comparison}
