% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_embedding.R
\name{compare_embedding}
\alias{compare_embedding}
\alias{compare_embedding.synth_pair}
\alias{compare_embedding.default}
\title{Compare Dimensionality Reduction Methods (t-SNE, UMAP, MDS-Sammon) for Two Datasets}
\usage{
compare_embedding(X, ...)

\method{compare_embedding}{synth_pair}(X, ...)

\method{compare_embedding}{default}(
  X,
  Y,
  vars,
  method = "tsne",
  perplexity = 30,
  n_neighbors = 15,
  side_by_side = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{X, Y}{data.frame or data.table: datasets to compare.}

\item{...}{additional arguments passed to methods}

\item{vars}{Character vector: numeric variables for dimensionality reduction.}

\item{method}{Character: one of 'tsne', 'umap', or 'mds'.}

\item{perplexity}{Numeric: t-SNE perplexity (default: 30).}

\item{n_neighbors}{Numeric: UMAP neighbors (default: 15).}

\item{side_by_side}{Logical: if TRUE, plots embeddings for X and Y side by side.}

\item{seed}{Integer: random seed for reproducibility of stochastic methods (t-SNE, UMAP).
Default NULL uses current random state.}
}
\value{
A list containing the embeddings and a ggplot2 visualization.
}
\description{
This function compares two datasets using dimensionality reduction methods: t-distributed
Stochastic Neighbor Embedding (t-SNE), Uniform Manifold Approximation and Projection (UMAP),
and Multidimensional Scaling (MDS) with Sammon's mapping. It allows visualizing complex
data structures, such as clusters or outliers, in two-dimensional space.
}
\examples{
# MDS (Sammon) embedding - doesn't require optional packages
set.seed(123)
X <- data.frame(
  var1 = rnorm(100, 50, 10),
  var2 = rnorm(100, 40, 8),
  var3 = rnorm(100, 30, 5)
)
Y <- data.frame(
  var1 = rnorm(100, 48, 12),
  var2 = rnorm(100, 42, 9),
  var3 = rnorm(100, 28, 6)
)
res_mds <- compare_embedding(X, Y, vars = c("var1", "var2", "var3"),
                             method = 'mds')

\donttest{
# t-SNE (requires Rtsne package)
if (requireNamespace("Rtsne", quietly = TRUE)) {
  res_tsne <- compare_embedding(X, Y, vars = c("var1", "var2", "var3"),
                                method = 'tsne')
}

# UMAP (requires uwot package)
if (requireNamespace("uwot", quietly = TRUE)) {
  res_umap <- compare_embedding(X, Y, vars = c("var1", "var2", "var3"),
                                method = 'umap')
}
}
}
\seealso{
Other comparison: 
\code{\link{ci_overlap}()},
\code{\link{compare_boxplots}()},
\code{\link{compare_chisq_gof}()},
\code{\link{compare_correlation_matrices}()},
\code{\link{compare_distributions_cont}()},
\code{\link{compare_feature_importance}()},
\code{\link{compare_histograms}()},
\code{\link{compare_ks_test}()},
\code{\link{compare_means_frequencies}()},
\code{\link{compare_missing_values}()},
\code{\link{compare_model_performance}()},
\code{\link{compare_multivariate_distribution}()},
\code{\link{compare_multivariate_summary_statistics}()},
\code{\link{compare_outliers}()},
\code{\link{compare_pca}()},
\code{\link{compare_wasserstein}()},
\code{\link{densitydiff_1d_num}()},
\code{\link{densitydiff_kl_num}()},
\code{\link{densitydiff_pca}()}
}
\author{
Matthias Templ
}
\concept{comparison}
