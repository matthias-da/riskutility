% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mia.R
\name{mia_classifier}
\alias{mia_classifier}
\title{Membership Inference Attack metric via classification}
\usage{
mia_classifier(
  real_data,
  synt_data,
  hout_data,
  cat_cols = NULL,
  num_cols = NULL,
  method = "rf",
  num_eval_iter = 5L,
  seed = NULL,
  num_trees = 300L,
  num_threads = 1L
)
}
\arguments{
\item{real_data}{data.frame. Records used to train the synthetic generator.}

\item{synt_data}{data.frame. Synthetic records produced by the generator.}

\item{hout_data}{data.frame. Holdout records from the same population,
NOT used to train the generator.}

\item{cat_cols}{character vector of categorical column names (optional).
If NULL, inferred as non-numeric columns.}

\item{num_cols}{character vector of numeric column names (optional).
If NULL, inferred as numeric columns.}

\item{method}{character. Classification back-end to use.
Currently supported: "rf" (random forest via ranger).}

\item{num_eval_iter}{integer. Number of evaluation iterations with different
holdout splits. Default 5.}

\item{seed}{integer or NULL. Random seed for reproducibility.}

\item{num_trees}{integer. Number of trees (only used for method = "rf").}

\item{num_threads}{integer. Threads for parallelism. Default 1.}
}
\value{
Named list with MIA precision, recall, macro F1 (means and SEs).
\itemize{
\item MIA recall is the primary privacy metric.
\item The evaluation test set is balanced (50/50).
\item Values near 0.5 indicate performance close to random guessing (low risk).
\item Values > 0.5 indicate the synthetic data leaks training membership.
\item Values < 0.5 indicate performance worse than random guessing.
}
}
\description{
Membership Inference Attack metric via classification
}
