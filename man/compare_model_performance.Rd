% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_model_performance.R
\name{compare_model_performance}
\alias{compare_model_performance}
\alias{compare_model_performance.synth_pair}
\alias{compare_model_performance.default}
\title{Compare Predictive Model Performance between Two Datasets}
\usage{
compare_model_performance(X, ...)

\method{compare_model_performance}{synth_pair}(X, ...)

\method{compare_model_performance}{default}(
  X,
  Y,
  formula,
  method = "rpart",
  metric = ifelse(is.factor(X[[as.character(formula[[2]])]]), "Accuracy", "RMSE"),
  trControl = NULL,
  ...
)
}
\arguments{
\item{X}{Original data.frame or data.table.}

\item{...}{additional arguments passed to methods}

\item{Y}{Synthetic or anonymized data.frame or data.table.}

\item{formula}{Formula specifying the model structure.}

\item{method}{A string specifying the model type (e.g., 'rf' for random forest, 'rpart' for CART). See \code{caret} package documentation.}

\item{metric}{Metric for model tuning (default 'Accuracy' for classification, 'RMSE' for regression).}

\item{trControl}{trainControl object from \code{caret} specifying cross-validation strategy (default 10-fold CV).}
}
\value{
List containing trained models, performance metrics for X and Y, and comparison results.
}
\description{
This function evaluates and compares predictive model performance between an original dataset (X) and a synthetic/anonymized dataset (Y). The model is trained separately within each dataset using cross-validation, and various performance metrics are computed.
}
\examples{
\donttest{
if (requireNamespace("caret", quietly = TRUE)) {
library(caret)
set.seed(123)
X <- data.frame(income = rnorm(500, 50000, 10000),
                age = rnorm(500, 40, 10),
                gender = factor(sample(c("M", "F"), 500, replace = TRUE)),
                target = factor(sample(c("Yes", "No"), 500, replace = TRUE)))

Y <- data.frame(income = rnorm(500, 48000, 12000),
                age = rnorm(500, 42, 11),
                gender = factor(sample(c("M", "F"), 500, replace = TRUE)),
                target = factor(sample(c("Yes", "No"), 500, replace = TRUE)))

result <- compare_model_performance(X, Y,
           formula = target ~ income + age + gender,
           method = 'rpart',
           metric = 'Accuracy')

print(result$comparison)
}
}
}
\seealso{
Other comparison: 
\code{\link{ci_overlap}()},
\code{\link{compare_boxplots}()},
\code{\link{compare_chisq_gof}()},
\code{\link{compare_correlation_matrices}()},
\code{\link{compare_distributions_cont}()},
\code{\link{compare_embedding}()},
\code{\link{compare_feature_importance}()},
\code{\link{compare_histograms}()},
\code{\link{compare_ks_test}()},
\code{\link{compare_means_frequencies}()},
\code{\link{compare_missing_values}()},
\code{\link{compare_multivariate_distribution}()},
\code{\link{compare_multivariate_summary_statistics}()},
\code{\link{compare_outliers}()},
\code{\link{compare_pca}()},
\code{\link{compare_wasserstein}()}
}
\concept{comparison}
