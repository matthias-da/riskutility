% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/densitydiff_kl.R
\name{densitydiff_kl_num}
\alias{densitydiff_kl_num}
\title{Kullback-Leibler divergence}
\usage{
densitydiff_kl_num(X, Y, stepsize = 1000)
}
\arguments{
\item{X}{a numeric vector or a matrix or data frame with numeric entries.}

\item{Y}{a numeric vector or a matrix or data frame with numeric entries.}

\item{stepsize}{number of interval points where the density is evaluated.}
}
\value{
The Kullback-Leibler divergence of \code{X} and \code{Y}.
}
\description{
Kullback-Leibler divergence between two variables or two matrices/data.frames.
}
\details{
First the probability distributions are estimated before the
Kullback-Leiber divergence is calculated.

The Kullback-Leibler divergence is defined as:
\deqn{KLD(X,Y) = \sum{X \times \log\left(\frac{X}{Y}\right)}}

If the data are multivariate, a Kullback-Leibler inspired divergence
is calculated through

\deqn{KLD(X,Y) = \sum{X \times \log\left(\frac{X}{Y}\right)}}
\deqn{KLD(X,Y) = \sum_{i,j}{X_{i,j} \left( \log\left(\frac{X_{i,j}}{Y_{i,j}}\right) +
\log(Y) - \log(X) \right)}}
with \deqn{X = \sum{X_{i,j}}} and \deqn{Y = \sum{Y_{i,j}}}
}
\examples{
x <- rnorm(100)
y <- rnorm(100)

densitydiff_kl_num(x, y)

X <- MASS::mvrnorm(100, mu = c(0,0), Sigma = diag(2))
Y <- MASS::mvrnorm(100, mu = c(0,0), Sigma = diag(2))
densitydiff_kl_num(X, Y)

X <- MASS::mvrnorm(100, mu = c(0,0,0), Sigma = diag(3))
Y <- MASS::mvrnorm(100, mu = c(0,0,0), Sigma = diag(3))
densitydiff_kl_num(X, Y)

}
\author{
Matthias Templ
}
