% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_internal.R
\name{.validate_pair_inputs}
\alias{.validate_pair_inputs}
\title{Shared input validation for two-dataset (X vs Y) risk measures}
\usage{
.validate_pair_inputs(
  X,
  Y,
  holdout = NULL,
  holdout_fraction = NULL,
  vars = NULL,
  na.rm = TRUE,
  seed = NULL,
  check_types = TRUE,
  min_vars = 1L
)
}
\arguments{
\item{X}{data.frame of original data}

\item{Y}{data.frame of synthetic data}

\item{holdout}{data.frame or NULL; if NULL and \code{holdout_fraction} is
not NULL, X is split into train + holdout}

\item{holdout_fraction}{numeric in (0, 1) or NULL (no holdout splitting)}

\item{vars}{character vector of variable names or NULL (auto-intersect)}

\item{na.rm}{logical, remove incomplete cases}

\item{seed}{integer or NULL, random seed for holdout sampling}

\item{check_types}{logical, check that variable classes match in X and Y
(default TRUE)}

\item{min_vars}{integer, minimum number of variables required (default 1)}
}
\value{
list with X, Y, vars (character), and optionally train, holdout
(if holdout splitting was performed)
}
\description{
Validates data frames, intersects variables, checks types, removes NAs,
and optionally splits holdout.  Used by nnaa(), singling_out(),
linkability(), domias(), hitting_rate(), and epsilon_identifiability().
}
\keyword{internal}
